{
  "name": "feeds-cli",
  "description": "UNIX philosophy-based news feed CLI for collecting and managing feeds locally.",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  },
  "dependencies": {
    "feedsmith": "^3.0.0-next.6"
  }
}
